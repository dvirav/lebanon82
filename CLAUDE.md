# חדר מצב 1982: סימולטור סקירה על מלחמת לבנון הראשונה

הקובץ הזה מעביר את כל ההקשר משיחה ב־claude.ai. קרא אותו כולו לפני שאתה נוגע בקוד.

## המטרה של הריפו הזה

להעביר את `reference/index.html` (שרץ היום כ־Artifact ב־claude.ai) לאתר אינטרנט עצמאי על **Render**, עם אותו סטאק כמו PLO Advisor (FastAPI בצד השרת). הסיבה העיקרית למעבר: ב־claude.ai כל צוער צריך חשבון Claude ושיתוף במייל כדי להצביע מהטלפון. באתר רגיל, כל אחד סורק QR ומצביע.

## ההקשר: בשביל מה זה

- דביר הוא צוער בקורס קציני סייבר. זו **התנסות**: סקירה של **15 דקות** על מלחמת לבנון הראשונה, לעד 15 צוערים, בכיתה, ואחריה משוב. נקודת הממשק הפיקודית: מפקד הא' סק"מ.
- **סדרי עדיפויות שדביר הגדיר:**
  1. הזמנים חשובים מאוד. חריגה מ־15 דקות = כישלון.
  2. **הלקחים חשובים יותר מהדיונים.** אם צריך לקצר, מקצרים את הדיונים ולא את הלקחים.
- הקהל כמעט לא מכיר את המלחמה, ולכן יש חלק רקע קצר.
- **תפקידי המכשירים:** מחשב מחובר למקרן ("מסך מקרן"), הטלפון של דביר ("שלט", הוא קובע את הקצב), הטלפונים של הצוערים ("צופה": עוקבים ומצביעים).

## איך הקובץ הנוכחי עובד (`reference/index.html`)

קובץ HTML יחיד, בלי build. כל התוכן נמצא בראש ה־`<script>` בקבועים: `PEOPLE`, `SEGMENTS`, `ITEMS`, `COVER_NOTE`. שאר הקוד הוא מנוע, והמסך, השלט והתסריט להדפסה נבנים כולם מהנתונים האלה. **לשנות תוכן = לשנות רק את הנתונים.**

### מצבים (`mode`)
- `screen`: צ'אט בגדול, מסך פתיחה עם QR, שקפי לקחים על כל המסך. אפשר גם לשלוט ממנו במקלדת או בקליקר.
- `remote`: שעון כולל מול 15:00, שעון לכל חלק מול התקציב שלו, צפי סיום ("מאחור ב־X, לקצר לפני הלקחים"), "מה להגיד עכשיו", "אם מאחרים", ספירת קולות עם +/− להרמת ידיים, וכפתורי הבא/חזרה.
- `viewer`: הצ'אט בטלפון, והצבעה בלחיצה כשסקר פתוח.
- `local`: מצגת עצמאית בלי סנכרון (גיבוי).
- `script`: תסריט להדפסה שנבנה מהנתונים.

### מודל ה־state (משותף לכל המכשירים)
```
{seq, rid, step, started, segStart:{segIdx:ms}, manual:{pid:[..]}, frozen:{pid:[..]}}
```
- `step` = כמה פריטים מ־`ITEMS` מוצגים (0 = מסך הפתיחה).
- `seq`/`rid` = שעון לוגי (Lamport): כל שינוי מקבל `seq = state.seq + 1`, והחדש מנצח (`newer()`). **לא** להשתמש ב־Date.now() להשוואה, כי השעונים של הטלפון ושל המחשב לא מסונכרנים.
- סקר פתוח **רק** כשהוא הפריט האחרון שמוצג (`livePollId`). "הבא" סוגר אותו, ו־`commit()` מקפיא את הספירה ל־`frozen`.
- `manual` = הרמות ידיים שדביר מוסיף בשלט. סה"כ = טלפונים + ידיים.
- `clean()` מוודא תקינות של כל state נכנס. לשמור על זה, כי זה קלט לא אמין.
- השלט והמסך שומרים את ה־state ב־localStorage (תקף 3 שעות), כדי לשרוד רענון.

### התלות ב־claude.ai שצריך להחליף
הקוד משתמש ב־`window.claude.use('room')` וב־`window.claude.use('user')`. בפועל הוא משתמש רק במעט מה־API:
- `room.emit('state', state)` / `room.on('state', fn)`: שידור ה־state. רק מי שהוא admin יכול לשדר.
- `room.presence({role, vote})` / `room.peers()` / `room.onPeers(fn)`: כל טלפון מפרסם את ההצבעה שלו ב־presence, והמסך והשלט סופרים מתוך `peers()`.
- `room.connected()` / `room.onConnection(fn)`
- `user.canEdit()`: האם המכשיר הוא של המציג.
- בלי `window.claude` בכלל, הקובץ עובר ל־`local` (מצגת עצמאית).

## הארכיטקטורה המומלצת לאתר

**Render Web Service אחד**, עם FastAPI שמגיש את הדף ו־WebSocket ב־`/ws`. ה־state נשמר בזיכרון (מופע אחד בלבד, בלי scaling).

### הגישה שדורשת הכי מעט שינויים (מומלצת)
לכתוב `sync.js` שבונה אובייקט `room` ואובייקט `user` **עם אותו ממשק בדיוק**, מעל WebSocket, ולהגדיר `window.claude = {use}` לפני הסקריפט הראשי. ככה מנוע הדף כמעט לא משתנה. `tests/sync_test_mock_room.py` כבר עושה בדיוק את זה עם BroadcastChannel, והוא תבנית טובה.

### השרת
- `GET /` מחזיר את `index.html`, ו־`GET /healthz` מחזיר ok.
- `WS /ws?role=...&key=...`: אם `key == PRESENTER_KEY` (משתנה סביבה ב־Render), החיבור הוא admin ורשאי לשדר `state`. אחרת הוא viewer.
- השרת שומר את ה־state האחרון ושולח אותו לכל מי שמתחבר, כך שמי שמאחר מקבל מיד את המיקום הנוכחי (זה מחליף את השידור החוזר כל 8 שניות).
- **הצבעות:** עדיף שהשרת יספור לפי `clientId` (מזהה אקראי שנשמר ב־localStorage של הטלפון). כך רענון לא מכפיל קול, ומי שיצא לא מוריד קול. השרת דוחה הצבעה לסקר שלא פתוח (לפי `step` ב־state ששמור אצלו).
- השרת לא סומך על שום קלט: לבדוק תקינות (אותו היגיון כמו `clean()`), להגביל גודל הודעה וקצב.

### תפקידים לפי URL
- `/` → צופה (זה מה שה־QR מקודד).
- `/?role=screen&key=XXX` → מסך מקרן. `/?role=remote&key=XXX` → שלט.
- מסך בחירת התפקידים יופיע רק למי שיש לו key.

### דברים שחייבים לזכור ב־Render
- בתוכנית החינמית השירות **נרדם אחרי כ־15 דקות בלי תנועה**, וההתעוררות לוקחת עד כדקה. לפתוח את האתר כמה דקות לפני המופע, או לעבור לתוכנית בתשלום ליום הזה.
- מופע אחד בלבד (ה־state בזיכרון).
- WebSocket נתמך ב־Render בלי הגדרות מיוחדות.
- **את ה־QR צריך לייצר מחדש** עם כתובת האתר. כרגע הוא מקודד את כתובת ה־Artifact ב־claude.ai. אפשר לייצר SVG עם `segno` ולהטמיע, כמו היום.

## החלטות תוכן שכבר התקבלו (לא לשנות בלי לשאול את דביר)

- **חלוקת זמנים (סה"כ 15:00):** פתיחה 1:00 · רקע 1:30 · פרוץ המלחמה 2:30 · מהלך הלחימה 3:00 · המצור על ביירות 2:30 · תוצאות 1:00 · לקחים וסגירה 3:30.
- **שלוש נקודות החלטה, עם נימוק בכל אחת:** (1) יוצאים למבצע אחרי הירי בארגוב? (2) היעד של 40 ק"מ הושג, מה עכשיו? (3) מה הייתם עושים במקומו של אלי גבע?
- **שלושת הלקחים שדביר בחר:**
  1. כל צעד נראה הגיוני. הסכום הוא 18 שנה. (זחילת משימה, נקודת החלטה 2)
  2. מדווחים אמת כלפי מעלה, גם כשהיא לא נוחה. (ההודעה שנמחקה)
  3. כשקצין טוב מבקש לרדת מפיקוד, זו נורה אדומה. (אלי גבע)
- הלקחים מוצגים **בסוף**, דביר מציג אותם (לא הצוערים), כל אחד כשקף מלא שמראה שוב את הרגע שממנו הוא נולד. הסגירה עונה על שאלת הפתיחה ("איך מבצע של 40 ק"מ הפך ל־18 שנה?") דרך שלושת הלקחים.
- **ההודעות מבוימות.** בעלי התפקידים מופיעים לפי תפקיד ולא בשם, ובמסך הפתיחה יש הבהרה שההודעות מנוסחות ואינן ציטוטים. להשאיר את זה כך.
- **"🚫 הודעה זו נמחקה"** היא המחשה, לא עובדה. בהערות למציג כתוב במפורש שהיסטוריונים טענו שהקבינט אישר מהלכים בלי תמונה מלאה, ושר הביטחון חלק על כך. לשמור על ההגינות הזאת.
- סברה ושתילה מנוסח בזהירות ("מאות אזרחים נרצחים", באזור שבשליטת צה"ל). לא להוסיף מספרים.
- מה שהוצא בכוונה כדי לחסוך זמן: ההתפטרות של בגין.

## בדיקות

- `tests/sync_test_mock_room.py` (Playwright): פותח מסך, שלט וצופים, מריץ את כל התסריט ובודק סנכרון, ספירת קולות, הקפאת סקר, מצטרף באיחור, איפוס ומצב עצמאי. אחרי המעבר ל־WebSocket, להתאים אותו לשרת אמיתי מקומי (`uvicorn`) במקום ה־mock.
- באג שחזר פעמיים: פריטי `news` בלי `from` הפילו את הרינדור. היום יש נרמול אחרי `ITEMS`. לשמור עליו.
- לפני המופע: ריצה אחת אמיתית עם המחשב והטלפון, ללחוץ "הבא", לוודא שהמסך זז, ואז "איפוס".

## קשור: הטופס "אישור תוכניות להתנסות"

מולא בנפרד (docx). **שים לב לפער:** בסעיף 11 בטופס החלוקה היא 5 חלקים (2/3/3/3/4 דקות), והסימולטור עבר ל־7 חלקים (למעלה). אם הטופס עוד לא הוגש, כדאי ליישר ביניהם.

## סגנון עבודה מול דביר

- עברית. הסברים בשכבות: קודם האינטואיציה, אחר כך הפרטים.
- לא לכתוב קוד לפני שהוא מאשר תוכנית, כשמדובר בשינוי גדול.
- לא לשנות תוכן היסטורי או לקחים בלי לשאול.
